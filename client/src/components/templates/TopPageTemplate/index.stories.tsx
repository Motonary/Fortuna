import * as React from 'react'
import { storiesOf } from '@storybook/react'
import TopPageTemplate from './index'

storiesOf('TopPageTemplate', module).add('TopPageTemplate', () => <TopPageTemplate />)
